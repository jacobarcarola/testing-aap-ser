---
- name: Listen for webhook events
  hosts: all
  sources:
    - ansible.eda.webhook:
        host: 0.0.0.0
        port: 5000
  rules:
    - name: Log any event
      condition: event.payload is defined
      action:
        debug:
          msg: "Received event: {{ event.payload }}"
